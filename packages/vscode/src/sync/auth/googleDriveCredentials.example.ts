/**
 * Google OAuth credentials template for LabShelf.
 *
 * The maintainer must copy this file to `googleDriveCredentials.ts` (same
 * directory) and fill in the real values from the Google Cloud Console.
 * `googleDriveCredentials.ts` is gitignored — never commit it.
 */
export const CLIENT_ID = 'YOUR_GOOGLE_OAUTH_CLIENT_ID';
export const CLIENT_SECRET = 'YOUR_GOOGLE_OAUTH_CLIENT_SECRET';
