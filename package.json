{
  "name": "labshelf-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev:vscode": "pnpm --filter @labshelf/vscode dev",
    "build:vscode": "pnpm --filter @labshelf/vscode build"
  }
}
